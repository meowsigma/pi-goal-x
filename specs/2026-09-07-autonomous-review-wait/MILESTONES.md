# Autonomous review/wait implementation log

## 2026-09-07 — investigation and approved direction

- Parent independently traced the status-loop incident and the released control flow. Ordinary no-work cycles always reach another checkpoint; coaching is not an enforced review transition. Completion auditing is executor-requested and gated by pending tasks.
- User chose full autonomy and asked to continue: real whole-goal review, concrete next work/final audit, or an owned quiet automatic recheck. Waiting must not become model-owned user pause or a fake completion.
- Read-only debugger failed at provider admission (retired configured model), before producing findings. No model substitution was made for that diagnosis. Findings above are parent evidence, not a successful debugger report.
- Created isolated worktree `/home/sigma/.pi/worktrees/pi-goal-x-review-wait`, branch `fix/review-before-repeat`, from released `1aa1b15bb878b517559748dd9156b25420c93a37`. Shared only the existing test dependency directory through an ignored node_modules symlink.
- Wrote PRODUCT and TECH. Compared stronger coaching, a separate scheduler/worker, and the selected existing runtime plus independent review. Kept stable model tools, final audit gate and human lifecycle ownership.
- Design self-review caught an important lifecycle boundary: an automatic audit started from agent_settled cannot rely on a future turn_end to archive. Plan now requires publishing the real result and invoking the shared archival operation, with full lifecycle proof.
- Parent baseline `b19e197dc` completed successfully: check and lint passed; 896/896 unit tests and 40/40 explicit-source integration tests passed, zero failures/skips. This is baseline evidence, not proof of the new behavior.
- Worker/reviewer preflight reports the existing user override `openai-codex/gpt-5.6-luna` for both builtin roles. The new implementation/review work uses that mapping; it is not a retry or substitution of the failed debugger diagnosis.

## Work remaining

- [x] Baseline verified from terminal output.
- [x] Incident-shaped lifecycle repair wired at agent_end → agent_settled: two no-progress runs request one independent review instead of another ordinary checkpoint. RED was captured by the pre-repair golden third-checkpoint assertions; updated lifecycle expectations now prove the second empty cycle does not enqueue another ordinary checkpoint.
- [x] Read-only structured reviewer and source-labelled user-decision context implemented using the real SDK session constructor seam; installed faux-provider SDK wiring is proven, while real external-provider reasoning remains NOT PROVEN.
- [x] Durable single-owner continuation state, persisted wake receipts, reload restoration, clamped timers, and quiet recovery admission implemented; fake-clock reload/due/stale-callback coverage is now proven by the focused runtime suite.
- [x] Actual reviewed task evidence uses GoalService invariants; audit disposition invokes the normal completion flow and shared post-settlement archive helper. The host-loaded faux-provider lifecycle proves review → task verification → audit → archive.
- [x] Existing unit/integration coverage preserves provider exhaustion, disabled settings, delegated ownership, budgets, cancellation, both NQA/goal loader orders, and native Escape behavior; the host-loaded scheduled-wait lifecycle now proves status preservation and one due wake in both orders.
- [x] Explicit-source NQA integration and native goal+BTW host-loader/TUI proof passed; the SDK 0.85.1 full run had 40/42 green with two missing-module failures before host-relative pi-ai resolution.
- [ ] Independent read-only review, parent validation and release decision remain parent-owned.

Final local proof: `npm run check`, `npm run lint`, `npm test` (898/898), explicit-source NQA integration (40/40), `npm run test:selfcheck` (898/898), native goal+BTW host-loader/TUI proof, and `git diff --check` passed. Test manifest refreshed with `node scripts/run-unit-tests.mjs --write-manifest`.

NOT PROVEN: real external-provider reasoning and universal model compliance. Installed faux-provider SDK lifecycle, fake-clock wake races, and host-loaded review/task/audit/archive control flow are proven. No live goals, user settings, installed sources, external accounts or protected BTW changes were modified. No release or installation is claimed.

## 2026-09-07 — targeted independent review repair

- Reproduced the independent review's five concrete RED findings from source: a `wait` result could coexist with pending tasks; review retry exhaustion could leave no recovery owner; retained actionable advice could buy another hot review loop; user-decision collection was not goal-scoped/provenance-bound; and malformed/empty-evidence persisted wakes could be announced as scheduled. Added behavioral coverage for each boundary (including stale transcript authority and overdue wake normalization).
- Repaired wait admission, exhausted-review quiet execution recovery with retained diagnostics, ignored-advice backoff, trusted goal/focus-scoped decision markers, and strict UTC/nonempty wake normalization. Due wake retirement now also updates in-memory state before a resumed review.
- GREEN proof: `npm run check` passed; `npm run lint` passed; `npm test` passed (901/901, zero failures/skips); explicit-source NQA integration passed (40/40, zero failures/skips); `node scripts/run-unit-tests.mjs --write-manifest` refreshed the official manifest; `npm run test:selfcheck` passed on rerun (901/901, zero failures/skips); `git diff --check` passed.
- Added tests: `tests/goal-review.test.ts` and `tests/goal-runtime-review-recovery.test.ts`. The RED evidence is the supplied read-only review reproduction; no private incident data or live account was used.
- Limits remain NOT PROVEN: real provider reasoning/SDK mock-provider lifecycle, fake-clock reload/due/stale-callback races, and the complete automatic review/task/audit/archive lifecycle. No release, push, install, settings, live goal, external account, or protected BTW change was performed.

## 2026-09-07 — targeted P1 snapshot and wake-retirement repair

- RED evidence: the independent review reproduced stale continuation persistence in `extensions/goal-review.ts` after a verified task update and ignored due-wake retirement persistence failure in `extensions/goal-runtime.ts`; both could respectively resurrect pending work or dispatch a still-durable wake. The supplied review was source-backed and did not inspect private incidents.
- Repaired continuation persistence to use the current post-`GoalService.updateTask` focused record, and made wake scheduling/retirement fail closed unless authoritative persistence succeeds. Failed retirement retains the durable lease and emits one bounded NOT PROVEN diagnostic without dispatch.
- Added behavioral tests for post-verification continuation state, missing persistence admission, failed retirement/no dispatch, and a real installed SDK `createAgentSession` with an in-memory faux provider that performs a real read, submits structured review output, exposes only read/grep/find/ls plus the submit tool, and exercises cancellation.
- GREEN proof: `npm run check` and `npm run lint` passed; `npm test` passed (905/905, zero failures/skips); explicit-source `npm run test:integration` passed (40/40, zero failures/skips); `node scripts/run-unit-tests.mjs --write-manifest` and `npm run test:selfcheck` passed; explicit-path native goal+BTW host-loader/TUI lifecycle proof passed; the targeted tests pass under the same runner; `git diff --check` passed. The official manifest remains current because no test entry file was added.
- Remaining NOT PROVEN: fake-clock reload/due/stale-callback race matrix and the complete automatic review/task/final-audit/archive lifecycle fixture. The faux-provider test proves SDK wiring and cancellation, not model reasoning. No release, push, install, settings, live goal, external account, or protected BTW change was performed.

## 2026-09-07 — clock probe parser and full entrypoint acceptance proof

- RED/GREEN clock coverage: `node --experimental-strip-types --test tests/goal-tool-names.test.ts tests/goal-progress-evidence.test.ts` passed 17/17. `isStandaloneClockProbe` now uses a narrow whitespace/simple-quote tokenizer and accepts plain `date`, `date -u`, `date --utc`, `date +%s`, and safe format variants while rejecting compound commands, date-setting options, unknown tools, shell operators, escapes, and substitutions. Paired history reconstruction exercises the same predicate across all accepted variants; real diagnostic/compound commands retain progress credit.
- Full entrypoint proof: `node --experimental-strip-types --test tests/integration/nqa-goal-host.test.ts` passed the actual host loader lifecycle test. In a temporary workspace, real `goalExtension` loading ran two `before_agent_start → agent_start → turn_start → turn_end → agent_end → agent_settled` cycles; the settled handler automatically invoked the real progress reviewer through installed `createAgentSession`, a registered faux provider read the artifact and submitted structured review, GoalService verified child then parent task evidence, the real `runGoalCompletionAuditor` used the same SDK/faux provider and returned the final `<approved/>`, and archival completed before the next executor turn. The test also proves no additional continuation was emitted after archival.
- The same command reports the existing two-order NQA host-loader test as skipped when `PI_NQA_EXTENSION_PATH` is absent. With the required explicit source, the NQA/goal ownership test passed in both load orders; a dedicated review/wait lifecycle under both NQA load orders remains NOT PROVEN because the native NQA fixture does not yet install the faux provider through that combined path. No claim is made for real external-provider reasoning. Existing negative verdict and pending-contract tests continue to assert active/non-archived state.
- Final validation after this change: `npm run check` passed; `npm run lint` passed; `npm test` passed 919/919 with zero failures/skips; `PI_NQA_EXTENSION_PATH=/home/sigma/.pi/worktrees/pi-nqa-integration-repair/extensions/no-questions-asked.ts npm run test:integration` passed 41/41 with zero failures/skips; `node scripts/run-unit-tests.mjs --write-manifest` refreshed the official manifest; `npm run test:selfcheck` passed 919/919; `PI_HOST_PACKAGE_ROOT=/home/sigma/.npm-global/lib/node_modules PI_BTW_EXTENSION_PATH=/home/sigma/.pi/worktrees/pi-learn-escape-repair/extensions/btw.ts node scripts/native-goal-btw-proof.mjs` passed; `git diff --check` passed. No staged files, installation, push, live goal/settings, external account, or protected BTW change was performed.

## 2026-09-07 — bounded P1 evidence/admission/prompt repair

- RED (before implementation): `node --experimental-strip-types --test tests/goal-progress-evidence.test.ts tests/goal-tool-names.test.ts tests/goal-runtime-review-recovery.test.ts tests/goal-review.test.ts` failed 4 tests: changing standalone date output earned tracker credit, history treated paired date results as progress, retained review clearing dropped `reviewFailures`, and the progress-review prompt omitted the goal-level contract.
- Implemented one narrow standalone clock-probe predicate shared by runtime evidence and history reconstruction. History now pairs assistant tool-call ids/arguments with results; unpaired or argument-missing history remains conservative, while compound bash and unknown tools retain credit. Added complete settled lifecycle coverage with changing date outputs and realistic paired reload-history fixtures.
- Preserved `reviewFailures` through meaningful-work advice clearing, added explicit exhausted progress-review admission, and retained explicit user/config/scope reset paths. Added a full four-failure flow test proving wake retirement plus productive work cannot buy a fifth reviewer provider attempt while the active goal remains available for safe work.
- Added an escaped dedicated `<goal_verification_contract>` section to the progress-review prompt without merging it into task contracts or authority-bearing decisions; the regression uses a distinctive goal-only contract.
- GREEN focused proof: `node --experimental-strip-types --test tests/goal-progress-evidence.test.ts tests/goal-tool-names.test.ts tests/goal-runtime-review-recovery.test.ts tests/goal-review.test.ts` passed 35/35; `node --experimental-strip-types --test tests/goal-review.test.ts tests/goal-runtime-review-recovery.test.ts` passed 19/19; `node --experimental-strip-types --test tests/goal-stale-continuation-golden.test.ts` passed 25/25. `npm run check`, `npm run lint`, and `git diff --check` passed after the repair.
- Full proof after the repair: `npm test` passed 918/918, explicit-source `PI_NQA_EXTENSION_PATH=/home/sigma/.pi/worktrees/pi-nqa-integration-repair/extensions/no-questions-asked.ts npm run test:integration` passed 40/40, `node scripts/run-unit-tests.mjs --write-manifest` refreshed the manifest, `npm run test:selfcheck` passed 918/918, and explicit-path native goal+BTW proof passed.
- Remaining proof gap: the exhaustion test drives `runGoalProgressReviewFlow` with an injected failing reviewer and does not claim proof of real provider reasoning or the full real entrypoint/provider-to-archive lifecycle. Parent must perform fresh full validation and independent review; no acceptance claim is made here.

## 2026-09-07 — focused wait/review lifecycle repair

- RED source evidence: the supplied independent review identified the settled handler re-adding its already-consumed review request (`extensions/goal-events.ts`), which could purchase another identical review after ignored advice. The new wait-admission test captures the pre-repair rejection of a dependency-blocked pending task, and the fake-clock timer test captures the pre-repair early-dispatch condition at the Node clamp; no separate pre-repair test process was run after the handoff, so those RED claims are source/reviewer evidence rather than an independently replayed command.
- Repaired the settled request root cause (consume once; no re-add), added explicit `wait.taskIds` covering every remaining pending task including parents, and made review scope canonical across disk round trips. Added operation-token, user-input epoch, authoritative reconciliation, and contract-scope guards around deferred review effects and nested usage accounting.
- Repaired deferred wakes with injectable fake-clock timers, generation-safe cancellation/replacement, long-delay re-arming, reload restoration, due-time single dispatch, and fail-closed retirement persistence.
- GREEN targeted proof: `npm run check`, `npm run lint`, `node --experimental-strip-types --test tests/goal-review.test.ts tests/goal-runtime-review-recovery.test.ts`, and `npm test` passed; targeted suite passed 17/17 and full suite passed 913/913 with zero failures/skips. The registered lifecycle test proves one purchased review across two empty cycles, ignored advice, and quiet recovery; the actual GoalService child→parent completion, independent approval, and immediate archive test passed; the pending-contract negative keeps the task pending and does not run the auditor. No separate review-specific auditor-rejection fixture was added because existing completion integration rejection coverage remains authoritative; real-provider reasoning and both-order host review/wait remain parent-owned NOT PROVEN.
- Final validation: `PI_NQA_EXTENSION_PATH=/home/sigma/.pi/worktrees/pi-nqa-integration-repair/extensions/no-questions-asked.ts npm run test:integration` passed 40/40; `node scripts/run-unit-tests.mjs --write-manifest` refreshed the manifest; `npm run test:selfcheck` passed 913/913; explicit-path `native-goal-btw-proof.mjs` passed; `git diff --check` passed. No staged files were created.

## 2026-09-07 — final auditor decision-context acceptance repair

- Repaired the independent review finding by moving decision collection into `extensions/goal-user-decisions.ts`. Only the exact v1 goal-owned message record emitted by the native interactive/RPC input handler is accepted: version, kind, source, goal id, focused goal id, focus epoch, and nonempty text are validated. Ordinary role:user provenance, malformed custom records, and dialog ids without paired host provenance are rejected.
- Added the dedicated source-labelled `<user_decisions>` section to the final auditor prompt. Both automatic progress-review audit and direct `update_goal({status: "complete"})` now pass current decisions separately from executor claims and scope guards. Final auditor task rendering includes each task verification contract and evidence.
- Real installed SDK faux-provider integration now captures distinctive later interactive and RPC restrictions in the actual final auditor prompt for both automatic review flow and direct completion; regression tests cover forged role/provenance, malformed custom, and invented dialog records.
- Validation: focused TypeScript check/lint and 44-test review/auditor/host run passed (host run with explicit NQA source passed 3/3); no live goals, settings, installs, pushes, or staged files changed.
- Trusted-extension limitation remains explicit: arbitrary loaded extensions have full Node/PiAPI privileges and can forge even a correctly shaped host record or alter any goal. This repair does not claim cryptographic isolation; it only excludes untrusted synthetic transcript/dialog-shaped text and preserves the existing trusted-extension boundary.

## 2026-09-08 — P1 owned-wait and selected-host resolution repair

- RED: ordinary interactive/RPC status input entered `before_agent_start` and unconditionally cleared `continuation.wake`; the selected-host integration imported pi-ai from a sibling path and failed under installed SDK 0.85.1 when pi-ai was nested below pi-coding-agent. NQA's conditional active-goal guidance also competed with a scheduled wait when NQA loaded first.
- Repaired wait admission conservatively: status/lifecycle polling preserves the persisted wake id/deadline and suppresses early substantive continuation; explicit resume, scope/lifecycle commands, and clear goal/work/objective changes still retire the lease safely. Added actual host-loaded status, due-wake, single-dispatch, both-order, and explicit-resume tests using the NEW NQA source path.
- Added the authoritative `[PI GOAL SCHEDULED WAIT]` branch. NEW NQA recognizes it when goal-x loads first and emits conditional defer guidance when NQA loads first; neither extension competes for the future continuation owner.
- Replaced sibling pi-ai imports with host-relative resolution from the selected coding-agent entry via `createRequire(...).resolve.paths`, package exports/import entry, and URL import. This supports nested installed and hoisted local layouts and keeps faux-provider registration on the selected host's provider registry.
- Honest validation: goal check/lint pass; explicit host integration passes with `PI_NQA_EXTENSION_PATH=/home/sigma/.pi/worktrees/pi-nqa-review-wait/extensions/no-questions-asked.ts` and with `PI_HOST_PACKAGE_ROOT=/home/sigma/.npm-global/lib/node_modules`; NEW NQA `npm test` passes. The parent-reported SDK 0.85.1 full validation was RED 40/42 only because the old sibling imports could not resolve; that gap is now addressed by source-relative resolution, while real external-provider reasoning and trusted-extension isolation remain NOT PROVEN.

## Parent regression repair and fresh validation

- Parent RED run `b228320c0` executed the explicit-source integration suite: 42/44 passed, two failed as expected. The new assertions caught `Do not change the goal` erasing a persisted wake and NQA-first ordinary ACTIVE inheriting an unconditional prohibition on substantive work.
- Parent repaired wait override classification to require anchored explicit command/imperative syntax and conservatively preserve waits for questions/negations. Removed unconditional wait deferral from the NQA conditional block; its scheduled-wait condition remains explicit. Restored the existing ACTIVE_GOAL_RECOVERY export.
- Parent GREEN run `ba12681f9`: check/lint passed, 44/44 integrations on each selected SDK loader (local 0.84.1 and installed 0.85.1), 920/920 unit tests, selfcheck, 83/83 NQA tests, and both diff checks passed, zero skipped tests. Integration assertions exercise both load orders, negative/question status variants, due wake ownership, and explicit resume.
- Earlier parent combined run `b18703527` also passed the native goal+BTW proof. Earlier installed-host import failure `b52e8c47a` is superseded by selected-host-relative dependency resolution and the fresh 44/44 run.
- Sources: goal review-wait worktree and NQA `/home/sigma/.pi/worktrees/pi-nqa-review-wait`. No release, install, staged files, live goal/settings mutations, or protected BTW edits. External model reasoning is not certified by faux-provider tests. Final narrow independent review `3c93376a-a179-43db-8c84-0798e2e91be6` returned QUALITY_VERDICT PASS with no findings on the final two repairs; this is a narrow review, not a new blanket full-codebase certification.
